<h1>New dose for cocktail</h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%#= f.input :ingredient_id, collection: Ingredient.all %>
  <%= f.submit "Add a dose", class: "btn btn-primary" %>
<% end %>
